import { styled } from "../../global/style/themes";

const Header = styled.header`
  font-size: 3.5em;
  font-weight: bold;
  text-align: center;
  padding-top: 50px;
  color: white;
`

export const HeaderStyles = {
  Header
}